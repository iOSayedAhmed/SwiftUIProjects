//
//  Article.swift
//  AricleRow
//
//  Created by Develop on 03/10/2022.
//

import Foundation
import UIKit

struct Article {
    let image = "forgetPassword"
    let title = "The Comperhansive Guide to the state Management in iOS"
    let author = "Elsayed Ahmed"
    let rating = 4
    let details = "A general rule of thumb is to always aim for the smallest accessibility scope possible. A local variable defined within a method is preferred over a global variable, not only for avoiding unexpected data mutations from other scopes that we didn’t notice at the first glance, but also for improving the testability of the modules that use that data."
}
